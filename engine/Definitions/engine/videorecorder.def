
#include "sbox/media/videorecorder.h"

native class CVideoRecorder as NativeEngine.CVideoRecorder
{
	static CVideoRecorder Create(); [new]
	void Destroy(); [delete]

	void Stop();
	void AddVideoFrame( CastTo[uint8*] void* rgbaData, long captureTimeMicroS );
	void AddAudioSamples( CAudioMixDeviceBuffers );

	bool Initialize( string filename, int width, int height, int fps, int bitRateMb, int audioSampleRate, int audioChannels, string videoCodec, string container, int encodingPreset, string audioCodec, bool transparency );
}
